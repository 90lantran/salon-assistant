# Scripts

Place small local scripts here for tasks like:

- seeding services
- exporting call logs
- loading demo appointments

The backend currently includes a runnable seed helper at:

- `/Users/ltran/vibe-coding/salon-assistant/backend/scripts/seed_demo_data.py`
